const upperCaseBase = 65; // 65 is the number of the 'A' in the ASCII TABLE
const lowerCaseBase = 97; // 97 is the number of 'a' in the ASCII TABLE
const regex = /\d|[~!@#$%^&*()_+\-=\[\]{}:";'<>?,./]/g

function shiftIt(){
    let input = document.getElementById("input").value
    let output = document.getElementById("output")
    let cipherText = "";
    let index = input.length - 1

    
}